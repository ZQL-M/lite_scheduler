<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tuba.schedulercore.mapper.TaskTriggerMapper">

    <!-- 根据任务 ID 查询触发器列表 -->
    <select id="selectByTaskId" resultType="com.tuba.schedulercore.model.TaskTrigger">
        SELECT * 
        FROM task_trigger 
        WHERE task_id = #{taskId} 
          AND deleted = 0
        ORDER BY priority DESC, created_time ASC
    </select>

    <!-- 查询所有启用的触发器 -->
    <select id="selectEnabledTriggers" resultType="com.tuba.schedulercore.model.TaskTrigger">
        SELECT * 
        FROM task_trigger 
        WHERE enabled = 1 
          AND deleted = 0
        ORDER BY priority DESC, created_time ASC
    </select>

    <!-- 根据触发器类型查询触发器 -->
    <select id="selectByTriggerType" resultType="com.tuba.schedulercore.model.TaskTrigger">
        SELECT * 
        FROM task_trigger 
        WHERE trigger_type = #{triggerType} 
          AND deleted = 0
        ORDER BY priority DESC, created_time ASC
    </select>

</mapper>
